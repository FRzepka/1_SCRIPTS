# SOC+SOH Tests (SOC_1.6.0.0 + SOH_0.1.2.3)

This folder contains test runs where SOH is estimated hourly by the SOH model (0.1.2.3) and then held constant for one hour to feed the SOC model (1.6.0.0). Each run is stored in a dated subfolder.

Run folders naming: `YYYY-MM-DD_NN`
